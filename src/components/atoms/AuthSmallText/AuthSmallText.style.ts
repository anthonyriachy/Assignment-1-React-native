import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container:{
        flexDirection: 'row',
        gap:4,
    },
    text:{
        fontSize: 16,
    },
    linkText:{
        fontSize: 16,
        fontWeight: 'bold',
    },
});
