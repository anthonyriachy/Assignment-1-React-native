import { StyleSheet } from 'react-native';

export const createStyles = () => StyleSheet.create({
  container: {
    width: '100%',
  },
  searchContainer: {
    paddingHorizontal: 16,
    marginBottom: 16,
  },
}); 